package com.tc.hutoolTest.language;

import cn.hutool.core.collection.ListUtil;
import cn.hutool.core.lang.Assert;

import java.util.List;

public class AssertTest {
    public static void main(String[] args) {

//        String a = "";
//        Assert.notBlank(a);

//        String b = null;
//        Assert.notNull(b);

//        List<Integer> list = ListUtil.list(false);
//        Assert.notEmpty(list);

//        boolean d = false;
//        Assert.isTrue(d);

        // 是子类和父类的关系
//        Assert.isAssignable(Action.class, Zhangsan.class);
//        Assert.isAssignable(Person.class, Zhangsan.class);

        //是否类实例
//        Zhangsan zhangsan = new Zhangsan();
//        Assert.isInstanceOf(Zhangsan.class, zhangsan);
//        Assert.isInstanceOf(Person.class, zhangsan);
//        Assert.isInstanceOf(Action.class, zhangsan);
    }
}
